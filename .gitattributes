# Git LFS tracking rules for CEO Digital Twin
# These ensure large binary files go through LFS on Hugging Face Spaces
# (HF Spaces supports LFS natively for repos up to 50GB)

# Vector store & SQLite databases
*.sqlite3 filter=lfs diff=lfs merge=lfs -text
*.db filter=lfs diff=lfs merge=lfs -text

# BM25 indexes and other pickled binary data
*.bin filter=lfs diff=lfs merge=lfs -text
*.pkl filter=lfs diff=lfs merge=lfs -text
*.pickle filter=lfs diff=lfs merge=lfs -text

# Parquet / Arrow files (if ingestion produces any)
*.parquet filter=lfs diff=lfs merge=lfs -text
*.arrow filter=lfs diff=lfs merge=lfs -text

# Model weight files (just in case any local weights are committed)
*.pt filter=lfs diff=lfs merge=lfs -text
*.pth filter=lfs diff=lfs merge=lfs -text
*.gguf filter=lfs diff=lfs merge=lfs -text
*.safetensors filter=lfs diff=lfs merge=lfs -text
