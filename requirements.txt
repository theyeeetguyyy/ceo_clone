# ══════════════════════════════════════════════════════════════════
# CEO Digital Twin — Python Dependencies
# These MUST match pyproject.toml version ranges.
# Pinned to minimum-bound so pip can resolve a coherent set.
# chromadb>=1.3.6 required — the vector_store was ingested with 1.x
# numpy<2.0 required — many libs still break on NumPy 2.x ABI changes
# ══════════════════════════════════════════════════════════════════

# ── Core LangChain / LangGraph stack ─────────────────────────────
langchain>=1.1.3,<2.0
langchain-core>=1.1.3,<2.0
langchain-community>=0.4.1,<1.0
langchain-text-splitters>=1.0.0,<2.0
langchain-groq>=1.1.1,<2.0
langgraph>=1.0.2,<2.0

# ── RAG / Embeddings ──────────────────────────────────────────────
sentence-transformers>=4.1.0,<6.0
chromadb>=1.3.6,<2.0
rank-bm25>=0.2.2

# ── LLM API ───────────────────────────────────────────────────────
groq>=0.9.0,<1.0

# ── Web framework ─────────────────────────────────────────────────
fastapi>=0.111.0,<1.0
uvicorn[standard]>=0.30.0,<1.0
python-multipart>=0.0.9
slowapi>=0.1.9

# ── Utilities ─────────────────────────────────────────────────────
python-dotenv>=1.0.0
pydantic>=2.7.0,<3.0
loguru>=0.7.2
numpy>=1.25.0