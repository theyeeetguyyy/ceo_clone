# Groq API keys (comma-separated for automatic rotation on rate limits)
GROQ_API_KEYS=gsk_your_key_1,gsk_your_key_2

# Optional: additional CORS origins (comma-separated)
# CORS_ORIGINS=https://your-custom-domain.com
